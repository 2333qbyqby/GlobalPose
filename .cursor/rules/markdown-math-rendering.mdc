---
description: 助手回复中的 LaTeX / 数学公式 Markdown 渲染约定（优先独立行 $$ 块公式）
alwaysApply: true
---

# 数学公式在 Markdown 中的写法

在面向用户的说明、推导、`docs/` 正文、以及聊天回复中需要写公式时，遵循下列规则，以便在 Cursor 等支持 KaTeX/MathJax 的预览里稳定渲染。

## 块公式（推荐默认）

- **使用独立行的美元符号双括号**，上下各单独一行，禁止与列表符号、引文或正文挤在同一行开头：

```markdown
千克力与加速度关系：

$$
\tau = J^\top f
$$
```

- **不要用** 只含一行的 `$$ \tau = ... $$` 贴在段落中间（部分渲染器易失效）；块公式前后各空一行更稳。

## 行内公式（次要、慎用）

- 若客户端对行内支持差，**优先改写成块公式**或纯文字（例如「其中 τ = Jᵀ f」）。
- 需要行内时可用单美元：`$\ddot q$`；避免与正常美元金额混淆时对符号用 Unicode（如 τ、ω）代替。

## 避免的写法（易不渲染）

- **避免**依赖 `\(...\)`、`\[` `\]` 作为唯一格式（部分内置 Markdown 预览不支持）。
- **避免**在列表项第一行直接写 `$$`（应单独起一段，或先空行再起公式块）。
- **避免**在代码块（fenced code）里放“要渲染”的公式——代码块内会当纯文本显示。

## 与代码引用的关系

- 说明仓库里的实现时：**公式仍用本规则的 Markdown 数学**；**源代码位置**继续用工作区要求的代码引用格式（```startLine:endLine:path），不要把路径写进 `$$` 公式块。

## 语言

- 用户要求中文回复时：公式旁的解释用**简体中文**；数学符号与国际惯例一致（粗体向量、矩阵大写等按需使用）。
