---
description: GlobalPose 项目核心约定与领域上下文
alwaysApply: true
---

# GlobalPose 核心规则

- 本仓库为 Python 项目，涉及人体姿态、IMU、动捕（如 Xsens、Noitom）与 `articulate` / `carticulate` 包；改动前先阅读邻近模块，保持与现有风格一致。
- 只改与任务直接相关的代码；避免无关重构、大范围格式化或新增未要求的文档。
- 新增脚本或配置时，避免写入他人机器特有路径；本地数据集路径用环境变量、参数或显式占位符说明。
- 数值与坐标约定以现有代码为准（例如四元数顺序 wxyz 等）；改数学或滤波逻辑时保持与张量形状、设备（CPU/GPU）现有用法一致。
